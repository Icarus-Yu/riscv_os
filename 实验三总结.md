- **Task 3: 物理内存分配器 (`kalloc.c`)**
    
    - **手册要求**: 实现 `kinit` (初始化), `kalloc` (分配), `kfree` (释放)，使用空闲链表管理。
        
    - **你的实现**:
        
        - `kinit()`: 遍历了从 `etext` 到 `PHYSTOP` 的内存并释放。
            
        - `kalloc()`: 实现了从 `freelist` 头部取页面的逻辑。
            
        - `kfree()`: 实现了头插法将页面放回链表。
            
    - **评价**: **完全达标**。你采用了 xv6 经典的空闲链表设计。
        
- **Task 5: 页表管理系统 (`vm.c`)**
    
    - **手册要求**: 实现 `walk` (页表遍历), `mappages` (建立映射), 并理解 Sv39 机制。
        
    - **你的实现**:
        
        - `walk()`: 正确实现了三级页表的遍历，当中间页表不存在且 `alloc=1` 时会自动分配新页并清零。
            
        - `mappages()`: 实现了虚拟地址到物理地址的映射，包含权限位的设置。
            
    - **评价**: **完全达标**。逻辑清晰，符合 RISC-V Sv39 标准。
        
- **Task 6: 启用虚拟内存 (`vm.c` & `main.c`)**
    
    - **手册要求**: 实现 `kvminit` (创建内核页表), `kvminithart` (写入 `satp` 寄存器并刷新 TLB)。
        
    - **你的实现**:
        
        - **映射策略**: 实现了恒等映射 (Identity Mapping)。
            
            - UART 设备 (0x10000000) -> 映射为 R|W。
                
            - 内核代码段 (0x80000000) -> 映射为 R|X。
                
            - 内核数据段 (etext ~ PHYSTOP) -> 映射为 R|W。
                
        - **激活**: 在 `main.c` 中按顺序调用了 `kinit` -> `kvminit` -> `kvminithart`。
            