### 一、 核心流程解读：功能是如何实现的？

实验六的核心目标是 **"让用户程序跑起来，并能请求内核办事（系统调用）"**。这涉及三个关键阶段：

#### 1. 创建第一个用户进程 (`kernel/proc.c`: `userinit`)

系统启动后，内核是“光杆司令”。我们需要手动创造第一个平民（用户进程）。

- **代码位置**：`kernel/proc.c` 中的 `userinit()` 和 `initcode` 数组。
    
- **实现逻辑**：
    
    - **`initcode[]`**：这是一个硬编码的二进制数组。它不是 C 代码，而是汇编指令的机器码。这段代码做了两件事：
        
        1. 将系统调用号 `SYS_write` (16) 放入 `a7`，参数放入 `a0-a2`，执行 `ecall`（打印 "Hello..."）。
            
        2. 将系统调用号 `SYS_exit` (2) 放入 `a7`，执行 `ecall`（退出）。
            
    - **`userinit()`**：
        
        1. 调用 `allocproc()` 分配一个 `struct proc`。
            
        2. **分配内存**：`kalloc()` 分配一页物理内存作为用户栈/代码空间。
            
        3. **拷贝代码**：将 `initcode` 数组拷贝到这页内存中。
            
        4. **建立页表**：`mappages` 将虚拟地址 `0` 映射到这页物理内存。
            
        5. **设置 Trapframe**：关键点！设置 `epc = 0`（程序计数器指向开头），`sp = PGSIZE`（栈指针指向页末尾）。
            
    - **状态变更**：将进程状态设为 `RUNNABLE`，这样调度器就能看见它了。
        

#### 2. 从内核态“跳”到用户态 (`kernel/trap/trampoline.S`)

调度器选中这个新进程后，通过 `swtch` -> `forkret` -> `usertrapret` -> `userret` 的链路执行。

- **关键代码**：`kernel/trap/trap.c` 中的 `usertrapret()` 和 `kernel/trap/trampoline.S` 中的 `userret`。
    
- **实现逻辑**：
    
    - **`usertrapret()`**：准备工作。它把内核的栈指针、内核页表地址填入进程的 `trapframe` 中（为了以后能跳回来）。
        
    - **`userret` (汇编)**：
        
        1. `csrw satp, a1`：**切换页表！** 从内核页表切换到用户页表。
            
        2. `ld ...`：从 `trapframe` 恢复用户通用的寄存器（a0, sp, ra 等）。
            
        3. `sret`：特权级指令，CPU 从 S 模式降级到 U 模式，跳转到 `sepc` 指向的地址（即虚拟地址 0，`initcode` 的开始）。
            

#### 3. 用户态“陷入”内核态（系统调用）

当 `initcode` 执行 `ecall` 指令时，CPU 产生异常，硬件自动跳转到 `stvec` 寄存器指向的地址（即 `uservec`）。

- **关键代码**：`kernel/trap/trampoline.S` (`uservec`) -> `kernel/trap/trap.c` (`usertrap`) -> `kernel/syscall.c` (`syscall`)。
    
- **实现逻辑**：
    
    - **`uservec` (汇编)**：保存用户寄存器到 `trapframe`，读取 `trapframe` 中的内核栈和内核页表，切换回内核页表，跳转到 `usertrap`。
        
    - **`usertrap` (C)**：检查 `scause` 寄存器。如果是 `8`，说明是系统调用。它将 `epc + 4`（为了返回时跳过 ecall 指令），开启中断，然后调用 `syscall()`。
        
    - **`syscall` (C)**：
        
        1. 从 `trapframe->a7` 读取系统调用号。
            
        2. 去 `syscalls[]` 函数指针数组里找对应的处理函数（比如 `sys_write`）。
            
        3. 执行函数，将返回值写回 `trapframe->a0`。
            

---

### 二、 最后的输出与工作的联系

让我们逐行分析你的 `make qemu` 输出（参考 `ex6总结.md` 和 `README.md`）：

1. **`userinit: created first user process`**
    
    - **来源**：`kernel/proc.c` 的 `userinit()` 函数末尾。
        
    - **意义**：证明手动构建的第一个进程（initcode）已经成功初始化，内存分配、页表映射都做完了，状态变成了 `RUNNABLE`。
        
2. **`scheduler: Starting scheduler...`**
    
    - **来源**：`kernel/proc.c` 的 `scheduler()` 函数开头。
        
    - **意义**：`main.c` 执行完毕，内核进入了死循环调度模式。它正在遍历进程表，应该马上就能发现刚刚创建的那个进程。
        
3. **`Hello, Syscall!`**
    
    - **来源**：这是**用户程序**打印的，不是内核打印的！
        
    - **完整链路**：
        
        1. 用户代码 `initcode` 执行 `li a7, 16` (SYS_write) 然后 `ecall`。
            
        2. 陷入内核 `sys_write` (`kernel/sysfile.c`)。
            
        3. `sys_write` 通过 `walk` 查用户页表，找到字符串 "Hello, Syscall!" 在物理内存的位置。
            
        4. 内核调用 `consputc` 把字符发给串口。
            
    - **意义**：**全链路打通的铁证**。证明了：用户态执行 -> 陷入内核 -> 参数传递 -> 地址翻译 -> 硬件操作 -> 返回用户态，这一整套极其复杂的流程全部正常工作。
        
4. **`[exit] Process 1 exited with status 0`**
    
    - **来源**：`kernel/proc.c` 的 `exit()` 函数。
        
    - **意义**：`initcode` 执行完打印后，调用了 `exit(0)`。内核接管后，将进程状态改为 `ZOMBIE`，并打印了这条消息。证明进程生命周期管理（创建->运行->销毁）是闭环的。
        

---

### 三、 新增代码和文件概览

为了实现上述功能，实验六主要涉及以下文件的新增和修改：

|**文件类别**|**文件名**|**关键变动与作用**|
|---|---|---|
|**新增核心逻辑**|**`kernel/syscall.c`**|**系统调用分发器**。定义了 `syscalls` 数组，实现了 `syscall()` 入口和 `argint` 参数提取函数。|
|**新增核心逻辑**|**`kernel/sysproc.c`**|**进程类系统调用实现**。实现了 `sys_fork`, `sys_exit`, `sys_wait`, `sys_getpid` 等对 `proc.c` 功能的封装。|
|**新增核心逻辑**|**`kernel/sysfile.c`**|**文件类系统调用实现**。实现了 `sys_write` 和 `sys_read`。重点在于处理用户指针（需要查页表）。|
|**重要修改**|**`kernel/proc.c`**|1. **`userinit`**: 手动加载 `initcode`。<br><br>  <br><br>2. **`fork/exit/wait`**: 完善了进程生灭逻辑。<br><br>  <br><br>3. **`allocproc`**: 修复了页表映射，把内核空间也映射给用户页表（为了跳板代码能运行）。|
|**重要修改**|**`kernel/trap/trap.c`**|1. **`usertrap`**: 增加了 `scause == 8` 的处理分支（系统调用）。<br><br>  <br><br>2. **`usertrapret`**: 设置进入用户态前的寄存器状态（sstatus, sepc 等）。|
|**重要修改**|**`kernel/trap/trampoline.S`**|取消了 `csrw satp` 的注释，真正开启了用户页表切换。实现了 `uservec` 和 `userret` 的上下文保存恢复逻辑。|
|**头文件**|**`include/syscall.h`**|定义了系统调用号宏（如 `SYS_write` 为 16）。|

---

### 四、 实验手册验收要求 vs. 我们的区别

参考《从零构建操作系统-学生指导手册.pdf》中 **实验6：系统调用 (Page 33-40)** 的要求，我们进行对比分析：

#### 1. 一致性 (Done Well)

- **任务1 (理解原理) & 任务2 (分发机制)**: 我们完全复刻了 xv6 的设计，使用 `a7` 传递调用号，`ecall` 触发异常，`syscall()` 进行分发 1。
    
- **任务4 (实现基础调用)**: 手册要求实现的 `fork`, `exit`, `wait`, `getpid`, `write` 我们都实现了 2。
    
- **任务6 (安全性)**: 手册要求检查用户指针 3。我们在 `sys_write` 中通过 `walk` 函数查询页表并检查 `PTE_V` 标志，确保了用户不能访问非法内存，符合要求。
    

#### 2. 区别与简化 (Differences)

- **用户程序的加载方式 (Task 5)**:
    
    - **手册要求**: 手册暗示使用 `usys.pl` 生成用户库，并且可能有更复杂的用户程序加载机制（可能涉及文件系统加载 ELF）。
        
    - **我们的做法**: 采用了 xv6 最早期的做法，即 **`initcode` 硬编码数组**。我们没有从磁盘加载 `.elf` 文件，而是直接把机器码写在 `proc.c` 里。
        
    - **原因**: 文件系统（Experiment 7）还没做，没法从磁盘读文件。这是合理的过渡方案。
        
- **文件系统调用的完整性**:
    
    - **手册要求**: `open`, `close`, `read`, `write` 4。
        
    - **我们的做法**: 实现了 `write` (fd 1/2) 和 `read` (fd 0, 控制台输入)。但由于没有真正的文件系统，`open` 和 `close` 实际上没法真正打开磁盘上的文件。目前的 `sys_write` 只是硬编码检查 `fd == 1` 就往串口打印。
        
    - **影响**: 这是一个“伪”文件系统接口，专门为了打印控制台设计的。真正的文件读写要等实验七。
        
- **`initcode` 的内容**:
    
    - 我们的 `initcode` 只是简单地打印 "Hello, Syscall!" 然后退出。而标准的 xv6 `initcode` 可能会尝试 `exec("/init")` 来启动 shell。因为我们还没实现 `exec` 和文件系统，所以这里简化为一个打印测试。