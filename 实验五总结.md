### 1. 实验五功能总结

实验五的核心目标是实现**进程抽象**和**抢占式调度**。具体完成的功能如下：

- **进程抽象 (Process Abstraction)**:
    
    - 定义了**进程控制块 (PCB)** (`struct proc`)，包含进程状态、PID、内核栈指针和上下文。
        
    - 定义了**进程状态** (`UNUSED`, `RUNNABLE`, `RUNNING` 等) 用于管理进程生命周期。
        
    - 定义了**上下文结构** (`struct context`)，用于保存被调用者保存寄存器 (callee-saved registers)。
        
- **上下文切换 (Context Switching)**:
    
    - 实现了汇编函数 `swtch`，用于在两个进程上下文（或调度器上下文）之间保存和恢复关键寄存器 (`ra`, `sp`, `s0`-`s11`)。
        
- **进程管理机制**:
    
    - 实现了进程表的初始化 (`procinit`)。
        
    - 实现了进程的创建与分配 (`allocproc`)，包括分配 PID 和内核栈，并预设上下文使得进程第一次被调度时能跳转到指定入口。
        
- **调度器 (Scheduler)**:
    
    - 实现了**轮转调度算法 (Round Robin)**，在 `scheduler()` 函数中无限循环，不断查找 `RUNNABLE` 状态的进程并进行切换。
        
    - 实现了**主动让出 CPU** (`yield`)，允许进程将状态改为 `RUNNABLE` 并交回控制权给调度器。
        
- **抢占式调度 (Preemption)**:
    
    - 修改了时钟中断处理逻辑。在 `kerneltrap` 中，当检测到时钟中断且当前有进程在运行时，强制调用 `yield()`，从而剥夺当前进程的 CPU 使用权，防止单一进程独占 CPU。
        

### 2. 新增与修改的文件

#### **新增文件**

- **`include/proc.h`**: 头文件。定义了 `struct proc` (进程结构体), `struct context` (上下文结构体), `enum procstate` (进程状态) 以及相关函数原型。
    
- **`kernel/proc.c`**: 核心 C 代码。实现了进程管理的全部高层逻辑，包括进程分配、调度器主循环等。
    
- **`kernel/proc/swtch.S`**: 核心汇编代码。实现了底层的上下文切换逻辑，直接操作寄存器。
    

#### **修改文件**

- **`kernel/main.c`**:
    
    - 新增了 `procinit()` 和 `create_test_proc()` 的调用。
        
    - 将最后的死循环 `while(1)` 替换为了 `scheduler()`，正式启动调度器。
        
- **`kernel/trap/trap.c`**:
    
    - 在时钟中断处理逻辑中（`interrupt_code == 5`），新增了对 `yield()` 的调用，实现抢占。
        
- **`kernel/mm/vm.c`**:
    
    - 在映射内核代码段时添加了 `PTE_W` 权限，这通常是为了方便调试或特殊需求（通常代码段应为 R+X，这里可能是为了写入断点或修改指令等特殊操作，或者仅是实验中的宽泛设置）。
        
- **`Makefile`**:
    
    - 可能更新了源文件列表以包含新的 `.c` 和 `.S` 文件。
        

### 3. 核心函数列表

|**函数名**|**文件位置**|**功能描述**|
|---|---|---|
|**`swtch`**|`kernel/proc/swtch.S`|**上下文切换的核心**。保存旧上下文的寄存器到内存，从新上下文加载寄存器到 CPU，实现执行流的跳跃。|
|**`scheduler`**|`kernel/proc.c`|**调度器主循环**。运行在每个 CPU 的特殊上下文中，负责从进程表中挑选一个 `RUNNABLE` 的进程并切换过去。|
|**`allocproc`**|`kernel/proc.c`|**分配进程**。查找空闲进程槽位，分配 PID，分配内核栈，并初始化上下文（设置 `ra` 为进程入口点 `proc_entry_point`）。|
|**`yield`**|`kernel/proc.c`|**让出 CPU**。将当前进程状态设为 `RUNNABLE`，并调用 `swtch` 切换回调度器上下文。|
|**`procinit`**|`kernel/proc.c`|**初始化**。遍历全局进程表 `proc[NPROC]`，将所有进程状态初始化为 `UNUSED`。|
|**`proc_entry_point`**|`kernel/proc.c`|**进程入口**。新进程第一次被调度时，`swtch` 返回后会跳转到这里。它负责开启中断并调用进程的具体任务函数。|
|**`create_test_proc`**|`kernel/proc.c`|**创建测试进程**。调用 `allocproc` 并将进程状态设为 `RUNNABLE`，用于演示多任务调度。|